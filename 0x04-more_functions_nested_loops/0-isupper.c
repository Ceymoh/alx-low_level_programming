#include "main.h"

/**
 * _isupper - checks for the uppercase character
 * @c: the character to print
 * Return: success 1 otherwise 0
 */

int _isupper(int c)

{
	if (c >= 'A' && c <= 'Z')

	return (1);

	else

	return (0);
}
